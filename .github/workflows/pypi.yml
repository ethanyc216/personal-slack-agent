name: Publish to PyPI

on:
  workflow_run:
    workflows:
      - Release
    types:
      - completed
  workflow_dispatch:
    inputs:
      tag:
        description: "GitHub Release tag to publish to PyPI, for example v0.1.9."
        required: true
        type: string

permissions:
  contents: read

jobs:
  publish-pypi:
    name: Publish GitHub Release to PyPI
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push' && github.event.workflow_run.head_branch == 'main')
    environment: pypi
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Resolve GitHub Release tag
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED_TAG: ${{ inputs.tag }}
          WORKFLOW_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          if [ -n "$REQUESTED_TAG" ]; then
            echo "tag=$REQUESTED_TAG" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [ -z "$WORKFLOW_HEAD_SHA" ]; then
            echo "No workflow_run head SHA was available." >&2
            exit 1
          fi

          tag="$(
            gh api "repos/${GITHUB_REPOSITORY}/releases" \
              --jq ".[] | select(.draft == false and .target_commitish == \"${WORKFLOW_HEAD_SHA}\") | .tag_name" |
            head -n 1
          )"

          if [ -z "$tag" ]; then
            echo "Could not find a GitHub Release targeting ${WORKFLOW_HEAD_SHA}." >&2
            exit 1
          fi

          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Download GitHub Release distributions
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
        run: |
          mkdir -p dist
          gh release download "$RELEASE_TAG" --pattern "*.whl" --dir dist
          gh release download "$RELEASE_TAG" --pattern "*.tar.gz" --dir dist
          ls -la dist

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
